import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      navText: "#ffffff",
      sectionText: "#ff8427",
    },
    secondary: {
      main: "rgb(112,34,90)",
    },
  },
});
